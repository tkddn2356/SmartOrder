<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.repository.UserMapper">

    <insert id="createUser">
		insert into user (account_id, password, name, nickname) values(#{account_id}, #{password}, #{name}, #{nickname})
	</insert>

	<select id="getUserByAccountId" resultType="com.example.domain.User">
		 select account_id, password, name, nickname from user where account_id= #{account_id}
	</select>

	<select id="getJwtTokenByAccountId" resultType="String">
		select jwt_token from user where account_id = #{account_id}
	</select>

	<update id="setJwtTokenByAccountId">
		update user
        set jwt_token = #{jwt_token}
        where account_id = #{account_id}
	</update>






</mapper>